### Date: 14/01/2026

## Define a function

define:

```r
myfuncC <- function (){

}
```
call the fn:

```r
myfuncC(argument)
```

sumof2num <- function(x,y){
    x+y
}

sumof2num(2,3)

markstograde : absolute and relative grading

install packages
install.packages("ProfessorR")
library("ProfessorR")

R packages in cran

hosted on Bioconductor : some packages related to biology
