Biostats -> data -> data types : normal, poisson, binomial

Poisson:
lambda 
assumptions: particular event at constant mean
eg. how many mutations in a year (unit is time)
how many locations (unit is spatial)

we can get probability of events. mean and variance (SD) is almost equal, the pattern is poisson dist.

## Binomial 
anything that have two outcomes: true/false, head/tail
probability = n(p), then other outcome = 1-p

## Uniform distribution
probability of any possibility is equal.

## data structures

vector= 1-D,
store only one type of data at once
it can store but cannot perform the functions


## data frame
make it tabular
