## Test Statistic

Sampling error
Samples from population
standardization

deviation from expected on null hypothesis
variance also dependent of H0

- Take ration between signal and noise
T = signal/noise
signal =  deviation
noise =  variance

signal more that noise, T will be more
more the W, in shapiro test, denominator is high

### The z-test/ z-statistic from a mean

population mean
actually sample the mean, check if matches population

For population sample:
Distribution or a model

from population, observe some values, match two assumptions:
IID : Independent and Identically Distributed

Both values are independent of each other, values of samples
can't predict the other values, they are independent in some way.
In real world, never independent of each other.

# Independent

If X1 is big, that does not make X2 is big.
In one family, similar may be due to gentics.

Non- Independece has to be removed.

## Indentically Distributed

All obs come from the same underlying distrubution.
There can be 4 differnt distrubution from each value.
All values should be same distrubution for combining.

Same mean, same variance, same shape.

Eg. the data used of gap minder.


The "known variance" assumption
Variance is small in z -test

The formula : signal/ratio
deviation from null Hyp

X bar - mu not = departure

X bar = sigma

Var(X bar) = sigma^2/n
variance should be less
so the sd of X is called the std error
SE =  

Expectation under null hyp, variability in estimates


### Numeric example


Say you measure, n = 25
Null H0: mu = 10
Known sigma = 4
Observed sample nean: X bar = 11.6

sigma/sqrt(n) = 4/5 = 0.8
Z = 11.6-10 / 0.8
Test stat = 2

Deviation is more, stronger null hyp


## Test stat (Z-stat) -> p-value

Normal distribution - PDF: Probability Density Function
in given axis, values in the position will be given

p-,d-,r-,n- norm

calculate area under the curve
from any test stat to p values, check the based stat and check  p-value

## PDF

For a normal distribution:
f(x) = 1/sigma. sqrt(2pie) e^0.5(x-mu/sigma)^2



Goal:
Plot the PDF of an ormal dist
show how chaning the mean shfits the cuce left, right
chande



1. A normal Dist PDF: 
seq fn (2:length) length is the difference





























