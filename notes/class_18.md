## t-stat

William Gosset

Ronald Fischer, started to use threshold of 0.05, two SD p-value

left or right tailed t-test, the t-value does not change.

confidence interval comparison

add p-value of two tails = 1, as covering both sides

# Two sample t -test

if variance is equal, pooled variance

for differende variance, S1/sqrt(n1) + S2/sqrt(n2)

## Midsem

Paired, unpaired
variance equal, unequal